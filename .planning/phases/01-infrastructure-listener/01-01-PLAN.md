---
phase: 01-infrastructure-listener
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, .env, .env.example, .gitignore, main.py, tests/test_webhook_security.py, tests/test_market_hours.py, tests/test_existing_endpoints.py, .planning/phases/01-infrastructure-listener/01-VALIDATION.md]
autonomous: false
requirements: [INF-01, INF-02, INF-03]
user_setup:
  - service: ngrok
    why: "Public tunnel for TradingView webhooks"
    env_vars:
      - name: WEBHOOK_SECRET_TOKEN
        source: ".env file (generate a strong random string)"
    dashboard_config:
      - task: "Create Free Static Domain"
        location: "Ngrok Dashboard -> Cloud Edge -> Domains"

must_haves:
  truths:
    - "POST /webhook returns 202 Accepted or 200 (asynchronously processed)"
    - "Requests from non-TradingView CIDR ranges are rejected (403)"
    - "Requests without valid WEBHOOK_SECRET_TOKEN are rejected (401)"
    - "System blocks signals outside BIST hours using pandas_market_calendars (XIST)"
    - "Existing endpoints (/dashboard, /portfolio, etc.) remain functional"
  artifacts:
    - path: "main.py"
      provides: "Refactored FastAPI app with security and market logic"
    - path: "tests/test_webhook_security.py"
      provides: "Security verification logic"
    - path: "tests/test_market_hours.py"
      provides: "Market hour verification logic"
    - path: "tests/test_existing_endpoints.py"
      provides: "Regression coverage for existing UI/API"
  key_links:
    - from: "main.py"
      to: ".env"
      via: "os.getenv for WEBHOOK_SECRET_TOKEN and CIDRs"
    - from: "main.py"
      to: "TradingView CIDRs"
      via: "ipaddress logic for filtering"
    - from: "main.py"
      to: "pandas_market_calendars"
      via: "XIST calendar check"
---

<objective>
Enhance the existing infrastructure with robust security, precise market hour logic, and comprehensive automated testing.
- Establish a test suite to ensure security and prevent regressions.
- Refactor `main.py` to include CIDR-based IP whitelisting for TradingView.
- Integrate `pandas_market_calendars` for accurate BIST (XIST) session checks.
- Preserve all existing UI and API endpoints (/dashboard, /portfolio, /trades, /signals, /scan, /config).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infrastructure-listener/01-RESEARCH.md
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment Setup</name>
  <files>.env, .env.example, .gitignore</files>
  <action>
    - Ensure `.env` and `.env.example` contain `WEBHOOK_SECRET_TOKEN` and placeholders for `ALLOWED_TRADINGVIEW_CIDRS`.
    - Update `.gitignore` to include `bot.log`, `.env`, `__pycache__/`, `.pytest_cache/`, and `*.db`.
    - Create the `tests/` directory if it doesn't exist.
  </action>
  <verify>
    <automated>ls .env .env.example .gitignore tests/</automated>
  </verify>
  <done>Basic environment files and directories prepared.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Security & Dependencies (Human-Verify)</name>
  <files>requirements.txt</files>
  <action>
    - Add `pandas-market-calendars`, `ipaddress`, and `pytest` to `requirements.txt`.
    - Human: Run `pip install -r requirements.txt`.
    - Human: Verify `pandas_market_calendars` is installed and `WEBHOOK_SECRET_TOKEN` is set in `.env`.
  </action>
  <what-built>Dependencies installed and environment verified.</what-built>
  <how-to-verify>
    1. Run `pip install -r requirements.txt`.
    2. Run `python -c "import pandas_market_calendars; import ipaddress; print('Security dependencies OK')"`
    3. Check `.env` for `WEBHOOK_SECRET_TOKEN`.
  </how-to-verify>
  <verify>Manual verification of dependencies and environment variables.</verify>
  <done>Environment and dependencies verified by user.</done>
  <resume-signal>Dependencies installed and environment verified.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test Suite Creation</name>
  <files>tests/test_webhook_security.py, tests/test_market_hours.py, tests/test_existing_endpoints.py</files>
  <behavior>
    - `test_webhook_security.py`: Verifies 403 for unknown IPs, 401 for bad tokens, and 200/202 for valid requests (using mocked IP/Token).
    - `test_market_hours.py`: Verifies logic that checks if BIST is open (mocking datetime/calendar).
    - `test_existing_endpoints.py`: Verifies `/dashboard`, `/portfolio`, `/trades`, `/signals`, `/scan`, `/config` return 200 OK.
  </behavior>
  <action>
    - Create `tests/test_webhook_security.py` using `fastapi.testclient`.
    - Create `tests/test_market_hours.py` verifying the BIST (XIST) schedule.
    - Create `tests/test_existing_endpoints.py` to ensure no regression on existing UI/API routes.
  </action>
  <verify>
    <automated>pytest tests/</automated>
  </verify>
  <done>Comprehensive test suite created and initial runs (expecting some failures until refactor).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Webhook & Market Hour Refactoring (Manual Edits)</name>
  <files>main.py</files>
  <behavior>
    - Test 1 (IP Whitelist): Requests from outside 52.89.214.238/32, 34.212.75.30/32, 54.218.53.128/32, 52.32.178.7/32 return 403.
    - Test 2 (Auth): Missing or wrong `WEBHOOK_SECRET_TOKEN` returns 401.
    - Test 3 (Market): Signal outside XIST hours (09:40-18:10 TR time) is rejected.
    - Test 4 (Preservation): All GET routes (/dashboard, etc.) remain functional.
  </behavior>
  <action>
    - **CRITICAL:** Do NOT overwrite `main.py`. Perform manual-style targeted edits to preserve existing logic.
    - Implement `is_ip_whitelisted(ip: str)` using `ipaddress`.
    - Implement `is_market_open()` using `pandas_market_calendars.get_calendar('XIST')`.
    - Update `receive_webhook` endpoint to:
      1. Check IP whitelist.
      2. Check Secret Token.
      3. Check `is_market_open()`.
    - Ensure the endpoint remains asynchronous and uses `BackgroundTasks` for heavy processing.
  </action>
  <verify>
    <automated>pytest tests/</automated>
  </verify>
  <done>main.py refactored with security and market hours; all tests passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Tunneling & Live Verification (Human-Verify)</name>
  <files>N/A</files>
  <action>Human verification of Ngrok tunnel and live webhook processing.</action>
  <what-built>Live tunnel and end-to-end webhook receipt.</what-built>
  <how-to-verify>
    1. Start FastAPI: `python main.py`.
    2. Start Ngrok: `ngrok http --url=YOUR_STATIC_DOMAIN 8000`.
    3. Send a test POST to `https://YOUR_DOMAIN/webhook` with valid JSON, Token, and (simulated) TradingView IP header if testing locally, or trigger a real TradingView alert.
    4. Verify `bot.log` shows the signal was received and processed (or rejected if outside market hours).
  </how-to-verify>
  <verify>Manual verification of live webhook flow.</verify>
  <done>Phase 1 verified live.</done>
  <resume-signal>Live verification complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internet → FastAPI | TradingView webhooks enter here. Untrusted input. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `/webhook` | Mitigate | CIDR-based IP Whitelisting (TradingView IPs) + WEBHOOK_SECRET_TOKEN check. |
| T-01-02 | Information Disclosure | Error Responses | Mitigate | Suppress detailed tracebacks in production; log internally. |
| T-01-03 | Availability | Webhook Handler | Mitigate | Use `BackgroundTasks` to prevent timeout-induced DoS or blocking. |
| T-01-SC | Tampering | pip installs | Mitigate | Human checkpoint for dependency verification (Task 2). |
</threat_model>

<verification>
- Full `pytest` suite execution.
- Manual verification of existing UI endpoints via browser.
- Ngrok live tunnel test.
</verification>

<success_criteria>
- FastAPI server successfully filters requests by CIDR and Secret Token.
- Market hours are correctly identified via `pandas_market_calendars`.
- Automated test suite covers security, market hours, and regressions.
- No regression in existing bot functionality.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-listener/01-01-SUMMARY.md` when done
</output>
